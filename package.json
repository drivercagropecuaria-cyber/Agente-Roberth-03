{
  "name": "orbit-monorepo",
  "version": "1.0.0",
  "description": "ORBIT Platform",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "node start.js"
  },
  "engines": { "node": ">=20.0.0" }
}
