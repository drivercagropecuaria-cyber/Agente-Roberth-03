{
  "name": "orbit-monorepo",
  "version": "1.0.0",
  "description": "ORBIT — Plataforma de Inteligência Aplicada",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "node start.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
